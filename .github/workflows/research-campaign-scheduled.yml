name: Scheduled Research Mainnet Replay Campaign

on:
  schedule:
    - cron: "2 7 * * *"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  research-campaign:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    concurrency:
      group: research-mainnet-replay-campaign
      cancel-in-progress: false
    env:
      COCOMELON_EXECUTION_MODE: paper
      COCOMELON_API_URL: https://api.hyperliquid.xyz
      COCOMELON_WS_URL: wss://api.hyperliquid.xyz/ws
      RESEARCH_CANDIDATE_ID: ${{ vars.RESEARCH_CANDIDATE_ID }}
      ATTEMPT_ID: research-${{ github.run_id }}-${{ github.run_attempt }}
      BATCH_ID: research-batch-${{ github.run_id }}-${{ github.run_attempt }}
      SOURCE_ID: research-mainnet-${{ github.run_id }}-${{ github.run_attempt }}
      GITHUB_RUN_ID: ${{ github.run_id }}
      GITHUB_RUN_ATTEMPT: ${{ github.run_attempt }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout research campaign control revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Prepare research campaign directories
        run: |
          set -euo pipefail
          mkdir -p \
            research-campaign/recording \
            research-campaign/output \
            research-campaign/state \
            research-campaign/diagnostics

      - name: Restore authoritative research registry
        run: |
          set -euo pipefail
          ARTIFACT_ROWS="$(
            gh api \
              "/repos/$GITHUB_REPOSITORY/actions/artifacts?name=research-authoritative-registry&per_page=100" \
              --jq '.artifacts
                | map(select(.expired == false and .workflow_run != null))
                | sort_by(.created_at)
                | reverse
                | .[]
                | [.id, .workflow_run.id, .workflow_run.head_branch, .workflow_run.head_sha]
                | @tsv'
          )"
          ARTIFACT_ID=""
          while IFS=$'\t' read -r CANDIDATE_ARTIFACT_ID RUN_ID ARTIFACT_BRANCH ARTIFACT_HEAD_SHA; do
            if [ -z "$CANDIDATE_ARTIFACT_ID" ] || [ -z "$RUN_ID" ]; then
              continue
            fi
            if [ "$ARTIFACT_BRANCH" != "main" ]; then
              continue
            fi
            RUN_IS_TRUSTED="$(
              gh api \
                "/repos/$GITHUB_REPOSITORY/actions/runs/$RUN_ID" \
                --jq '(.head_branch == "main")
                  and (.path == ".github/workflows/research-campaign-scheduled.yml")
                  and (.status == "completed")
                  and (.conclusion == "success" or .conclusion == "failure")
                  and (.event == "schedule" or .event == "workflow_dispatch")'
            )"
            RUN_HEAD_SHA="$(
              gh api \
                "/repos/$GITHUB_REPOSITORY/actions/runs/$RUN_ID" \
                --jq '.head_sha // empty'
            )"
            if [ "$RUN_IS_TRUSTED" != "true" ] || [ "$RUN_HEAD_SHA" != "$ARTIFACT_HEAD_SHA" ]; then
              continue
            fi
            ARTIFACT_ID="$CANDIDATE_ARTIFACT_ID"
            break
          done <<< "$ARTIFACT_ROWS"
          if [ -z "$ARTIFACT_ID" ]; then
            printf '%s\n' \
              'Trusted authoritative research registry unavailable; V4 registry completeness cannot be proven.' \
              > research-campaign/diagnostics/authoritative-registry-unavailable.txt
            exit 65
          fi
          gh api \
            "/repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip" \
            > research-campaign/state/research-authoritative-registry.zip
          unzip -q \
            research-campaign/state/research-authoritative-registry.zip \
            -d research-campaign/state/restored
          REGISTRY_PATH="$(find research-campaign/state/restored -type f -name research.sqlite3 -print -quit)"
          if [ -z "$REGISTRY_PATH" ]; then
            printf '%s\n' \
              'Authoritative registry artifact did not contain research.sqlite3; V4 registry completeness is unavailable.' \
              > research-campaign/diagnostics/authoritative-registry-unavailable.txt
            exit 66
          fi
          cp "$REGISTRY_PATH" research-campaign/state/research.sqlite3

      - name: Resolve candidate code revision from authoritative registry
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          import sqlite3
          from pathlib import Path

          candidate_id = os.environ.get("RESEARCH_CANDIDATE_ID", "").strip()
          if not candidate_id:
              raise RuntimeError("RESEARCH_CANDIDATE_ID repository variable is required")

          connection = sqlite3.connect(Path("research-campaign/state/research.sqlite3"))
          try:
              row = connection.execute(
                  "SELECT code_revision FROM research_candidates WHERE candidate_id = ?",
                  (candidate_id,),
              ).fetchone()
          finally:
              connection.close()
          if row is None or not isinstance(row[0], str):
              raise RuntimeError("research candidate is missing from authoritative registry")
          revision = row[0].strip().lower()
          if len(revision) != 40 or any(char not in "0123456789abcdef" for char in revision):
              raise RuntimeError("research candidate code_revision must be a 40-character commit SHA")
          github_env = Path(os.environ["GITHUB_ENV"])
          with github_env.open("a", encoding="utf-8") as handle:
              handle.write(f"RESEARCH_CODE_REVISION={revision}\n")
          PY

      - name: Persist acquisition attempt before candidate setup
        env:
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          from pathlib import Path

          from cocomelon.research.registry import ResearchRegistry
          from cocomelon.research.runner_history import record_runner_attempt_started

          candidate_id = os.environ.get("RESEARCH_CANDIDATE_ID", "").strip()
          if not candidate_id:
              raise RuntimeError("RESEARCH_CANDIDATE_ID repository variable is required")
          registry = ResearchRegistry(Path("research-campaign/state/research.sqlite3"))
          try:
              candidate = registry.load_candidate(candidate_id)
              if candidate.code_revision != os.environ["RESEARCH_CODE_REVISION"]:
                  raise RuntimeError("restored research candidate code revision changed")
              record_runner_attempt_started(
                  registry.connection,
                  attempt_id=os.environ["ATTEMPT_ID"],
                  candidate_id=candidate_id,
                  batch_id=os.environ["BATCH_ID"],
                  source_id=os.environ["SOURCE_ID"],
                  artifact_root="research-campaign/output",
              )
          finally:
              registry.close()
          PY

      - name: Checkout candidate code revision
        uses: actions/checkout@v4
        with:
          ref: ${{ env.RESEARCH_CODE_REVISION }}
          fetch-depth: 1
          clean: false

      - name: Install Cocomelon
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .

      - name: Validate candidate after setup
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          from pathlib import Path

          from cocomelon.research.registry import ResearchRegistry

          registry = ResearchRegistry(Path("research-campaign/state/research.sqlite3"))
          try:
              candidate = registry.load_candidate(os.environ["RESEARCH_CANDIDATE_ID"])
              if candidate.code_revision != os.environ["RESEARCH_CODE_REVISION"]:
                  raise RuntimeError("restored research candidate code revision changed during setup")
          finally:
              registry.close()
          PY

      - name: Acquire one public mainnet research cohort
        run: |
          set -euo pipefail
          printf '1\n' > research-campaign/output/acquisition-attempt.txt
          cocomelon record-mainnet-evidence \
            --root research-campaign/recording \
            --seconds 1200 \
            --deep-limit 5 \
            > research-campaign/output/record-transport.json

      - name: Build offline authenticated research cohort
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          import sqlite3
          from decimal import Decimal
          from pathlib import Path

          from cocomelon.evidence.bundle import load_baseline_replay_bundle
          from cocomelon.research.cohort import build_research_cohort

          build_research_cohort(
              "research-campaign/recording",
              "research-campaign/output",
              Decimal("10000"),
              trigger_head_sha=os.environ["GITHUB_SHA"],
          )
          bundle = load_baseline_replay_bundle("research-campaign/output/bundle.json")
          connection = sqlite3.connect(Path("research-campaign/state/research.sqlite3"))
          try:
              row = connection.execute(
                  "SELECT config_digest FROM research_candidates WHERE candidate_id = ?",
                  (os.environ["RESEARCH_CANDIDATE_ID"],),
              ).fetchone()
          finally:
              connection.close()
          if row is None or not isinstance(row[0], str):
              raise RuntimeError("research candidate config identity is unavailable")
          if row[0] != bundle.replay_config.config_digest:
              raise RuntimeError(
                  "research candidate semantic config does not match replay configuration"
              )
          PY

      - name: Evaluate authenticated research attempt
        run: |
          set -euo pipefail
          cocomelon-research-runner run-artifact \
            --registry research-campaign/state/research.sqlite3 \
            --attempt-id "$ATTEMPT_ID" \
            --candidate-id "$RESEARCH_CANDIDATE_ID" \
            --batch-id "$BATCH_ID" \
            --source-id "$SOURCE_ID" \
            --artifact-root research-campaign/output \
            > research-campaign/output/runner.json
          cocomelon-research-status \
            --registry research-campaign/state/research.sqlite3 \
            --format json \
            > research-campaign/output/status.json

      - name: Persist workflow failure in attempt ledger
        if: ${{ failure() }}
        run: |
          set -euo pipefail
          if [ ! -f research-campaign/state/research.sqlite3 ]; then
            exit 0
          fi
          python - <<'PY'
          import os
          import sqlite3
          from pathlib import Path

          connection = sqlite3.connect(Path("research-campaign/state/research.sqlite3"))
          try:
              cursor = connection.execute(
                  """
                  UPDATE research_runner_attempts
                  SET status = 'failed', start_ms = NULL, end_ms = NULL,
                      report_id = NULL, error_type = 'WorkflowFailure',
                      error_message = 'research campaign failed before authenticated checkpoint'
                  WHERE attempt_id = ? AND status IN ('running', 'evaluating')
                  """,
                  (os.environ["ATTEMPT_ID"],),
              )
              if cursor.rowcount not in (0, 1):
                  raise RuntimeError("research runner failure finalization was ambiguous")
              connection.commit()
          finally:
              connection.close()
          PY

      - name: Upload complete research campaign audit trail
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: research-campaign-${{ github.run_id }}-${{ github.run_attempt }}
          path: research-campaign/
          if-no-files-found: error
          retention-days: 30

      - name: Publish authoritative research registry
        if: ${{ always() && hashFiles('research-campaign/state/research.sqlite3') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: research-authoritative-registry
          path: research-campaign/state/research.sqlite3
          if-no-files-found: error
          retention-days: 30
